# Repository Instructions

## Release Versioning

- Do not bump version strings in project files when making a release.
- For version bumps, create and push a new `git tag` only.
- Follow the existing tag format, such as `v0.1.7`.
